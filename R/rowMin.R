#' @export
################################################################## 
## Returns the min each row of a dataset
##################################################################
rowMin <- function( Matrix ){
  Min <- apply(X = Matrix, MARGIN = 1, FUN = min)
  return(Min)
}
################################################################## 
