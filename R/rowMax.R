#' @export
################################################################## 
## Returns the max each row of a dataset
##################################################################
rowMax <- function( Matrix ){
  Max <- apply(X = Matrix, MARGIN = 1, FUN = max)
  return(Max)
}
################################################################## 
